# Auth

session · password · rate-limit (spec §11.2)

เทคโนโลยีที่ spec §11.1 กำหนดไว้แล้ว: custom credentials + `jose` (JWT)
+ ตาราง Session + `argon2` — ยังไม่ได้ติดตั้ง package เหล่านี้

ขอบเขตของ **P1**: login / logout / เปลี่ยนรหัสผ่าน / lockout
